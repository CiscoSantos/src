
// results will be freed

// lg is the number of fragments that you will return
// when the given string is encoded; it up to you to fill lg
char** encode(char* strng, int shift, int* lg) {
    // your code
}
// sz is the given number of fragments to decode
char* decode(char** s, int sz) {
    // your code
 }
