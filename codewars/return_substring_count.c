#include <stdio.h>
#include <string.h>


int search_substr(const char *full_text, const char *search_text, bool allow_overlap) {
 	
	
	for(int i=0; i < strlen(full_text); i++){
		if((*full_text+i)==* )
		{
			for(int j=0;j<strlen(search_text);j++)
			{
				if((*full_text+i+j)=
				{
					
				}
			}		
		}
	
	}
	     
	return -1;
}
